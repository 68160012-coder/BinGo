# SPEC — เว็บไซต์ของกลุ่ม

# ชื่อเว็บ: BinGo

# ปัญหาขยะที่แก้ (1 ประโยค): ระบุจุดที่มีถังขยะและจุดที่มีขยะ

# ผู้ใช้หลัก: (เช่น นักท่องเที่ยว / เทศบาลแสนสุข / ร้านค้าชายหาด)

# ฟีเจอร์ที่ต้องมี (ต้องทำได้ในบ่ายนี้ 2 อย่าง + ถ้ามีเวลา 1 อย่าง):
1. หน้าสถิติ: แสดง ... จากข้อมูลเปิด
2. หน้าแจ้งจุดขยะ: รับ ... บันทึกลง DB แสดงรายการล่าสุด
3. ระบุที่มีถังขยะใกล้ที่สุด และจุดถังขยะเต็ม

# ตาราง DB: (ใช้ waste_stats + reports เป็นฐาน เพิ่มคอลัมน์ได้)

# API: GET /api/stats.php , GET+POST /api/reports.php , (เพิ่ม) ...

# โมเดลธุรกิจ/ผู้ที่ได้ประโยชน์ (สำหรับ Pitch):

# ใช่ธีมสี 425B9A 76C0EC FFF6DC 

# 